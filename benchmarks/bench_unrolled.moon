type Player:
  name: "Emrys",
  my status:
  give my name
end

end
let p be Player {
}

let i be 0
let start be clock
while i < 10000:
  p's status
  p's status
  p's status
  p's status
  p's status
  p's status
  p's status
  p's status
  p's status
  p's status

  p's status
  p's status
  p's status
  p's status
  p's status
  p's status
  p's status
  p's status
  p's status
  p's status

  p's status
  p's status
  p's status
  p's status
  p's status
  p's status
  p's status
  p's status
  p's status
  p's status

  p's status
  p's status
  p's status
  p's status
  p's status
  p's status
  p's status
  p's status
  p's status
  p's status

  p's status
  p's status
  p's status
  p's status
  p's status
  p's status
  p's status
  p's status
  p's status
  p's status

  p's status
  p's status
  p's status
  p's status
  p's status
  p's status
  p's status
  p's status
  p's status
  p's status

  p's status
  p's status
  p's status
  p's status
  p's status
  p's status
  p's status
  p's status
  p's status
  p's status

  p's status
  p's status
  p's status
  p's status
  p's status
  p's status
  p's status
  p's status
  p's status
  p's status

  p's status
  p's status
  p's status
  p's status
  p's status
  p's status
  p's status
  p's status
  p's status
  p's status

  p's status
  p's status
  p's status
  p's status
  p's status
  p's status
  p's status
  p's status
  p's status
  p's status

  update i + 1
end
show clock - start
